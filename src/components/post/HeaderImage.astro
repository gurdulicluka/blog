---
import { Picture } from "@astrojs/image/components";

interface Props {
  image: {
    src: string;
    alt: string;
  };
}

const { image } = Astro.props;
const pictureBreakpoints =
  "(max-width:480px) 320w,(max-width:767px) 480w,(max-width:991px) 768w, (max-width:1199px) 992w, 1100w";
---

<figure
  x-data
  class="md:border-2 border-base-200 -mx-6 lg:-mx-0 md:rounded-lg h-[375px] md:h-[500px] overflow-hidden">
  <Picture
    data-image
    loading="eager"
    fetchpriority="high"
    src={image.src}
    alt={image.alt}
    aspectRatio="16:9"
    widths={[320, 480, 768, 992]}
    sizes={pictureBreakpoints}
    formats={["webp"]}
    class="w-full object-cover h-full object-center"
  />
</figure>
